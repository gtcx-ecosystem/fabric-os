# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
*.tsbuildinfo
*.zip

# Coverage
coverage/
.nyc_output/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.*.local

# Turbo
.turbo/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Terraform
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform lambda artifacts (built at deploy time)
04-ship/terraform/modules/secrets/lambda/rotation.zip
deploy/terraform/modules/secrets/lambda/rotation.zip

# Security reports (generated, not tracked)
04-ship/security/reports/

# Local archive (reference only, not tracked)
_archive/
_cannon/
_delete/

# Runtime artifact — baseline-os dist copied locally by session/tooling (never at root)
baseline-os/


# Local IDE settings
.claude/settings.local.json
.claude/skills/

# Generated security reports
04-ship/security/reports/rollback-evidence/
*.tfplan
**/tfplan.secrets
supabase/.temp/

# INF-86 ceremony — SPKI DER stays on secure path only (hash may be committed)
docs/audit/evidence/inf-86/**/*.pub.der
/secure/


# BaselineOS institutional memory (tracked)
.baseline/checkpoints/
.baseline/metrics/
.baseline/offline/
.baseline/archives/
.baseline/govern/
.baseline/index/
.baseline/data/
.baseline/evidence/
.baseline/keys/
.baseline/knowledge/
.baseline/security/

# Auto-added by ecosystem-self-heal
logs/
.next/
out/

# Local Baseline initialization artifacts (never tracked)
.baseline/config.json
.baseline/memory.db

# Generated DR fire-drill evidence placeholder (live dated artifact committed via operator run)
audit/dr-fire-drill-evidence-*.md

# Forbidden P35 v5 root extraction artifact
01-docs/
baseline.config.json

# Operator-local (never commit)
.kube-config-prod
.helm-cache/
.helm-config/

# Local Supabase CLI scratch
supabase/

# --- per-run telemetry churn (auto-added; keeps trees clean) ---
.baseline/forensic-ai/
.baseline/frame/
.baseline/session/
.baseline/experience/
machine/ci/settlement-commit-latest.json
machine/ci/settlement-push-latest.json
machine/ci/settlement-story-scope-latest.json
pm/ci/settlement-commit-latest.json
pm/ci/settlement-push-latest.json
pm/ci/settlement-story-scope-latest.json
workstream/sessions/ci/
workstream/sessions/ledger/
workstream/sessions/transcripts/
machine/ci/settlement-*-latest.json
pm/ci/settlement-*-latest.json
.baseline/memory/session-last-start.json
