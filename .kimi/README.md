# `.kimi/` — Kimi agent mirror

Kimi Code CLI agent configuration synced from repo `AGENTS.md` / `CLAUDE.md` partials.

| File        | Purpose                                                   |
| ----------- | --------------------------------------------------------- |
| `AGENTS.md` | Kimi-facing agent entry (generated via `pnpm agent:sync`) |

Regenerate after editing `.agent/*.md` sources: `pnpm agent:sync`.
