---
title: 'Deployment Open Items Audit'
status: current
date: 2026-06-30
owner: fabric-os
document_type: audit
source: /Users/amanianai/Sites/fabric.infra
---

# Deployment Open Items Audit

This audit reconciles `/Users/amanianai/Sites/fabric.infra` against current local
repo and read-only cloud checks on 2026-06-30.

## Summary

The GitHub-independent CI/CD strategy is implemented and pushed on
`fabric-os@feat/ai-cost-check`, but live staging bootstrap is still not complete.
The critical blockers are DNS authority for `gtcx.trade`, Terraform drift from
interrupted applies, Argo CD not installed, local disk exhaustion, and unpushed
cross-repo follow-up work in `nyota-ai` / `bridge-os`.

## Open Items

| ID          | Severity | Owner                          | Status                    | Open item                                                                                                                                                                                       | Evidence                                                                                                                                                                                                                                                            | Next action                                                                                                                                                                  |
| ----------- | -------- | ------------------------------ | ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-INFRA-001 | P0       | fabric-os / operator           | code-ready / live-pending | `griot-staging.gtcx.trade` ACM validation is still pending. `gtcx.trade` is Cloudflare-authoritative; staging now has a Cloudflare DNS Terraform path for Griot ACM validation + service CNAME. | `aws acm describe-certificate` shows `PENDING_VALIDATION`; DoH query for validation CNAME returns authority only; NS query returns `cora.ns.cloudflare.com` and `yichun.ns.cloudflare.com`; `terraform validate -no-color` passes after Cloudflare provider wiring. | Apply staging Terraform with `CLOUDFLARE_API_TOKEN` / optional `griot_ai_cloudflare_zone_id`, then re-check ACM validation and public DNS.                                   |
| F-INFRA-002 | P0       | fabric-os                      | blocked-network           | Staging Terraform state/drift is not cleanly audited after interrupted live applies.                                                                                                            | Transcript records killed/timeout applies and force-unlocks; current `terraform plan` cannot run because Terraform backend STS DNS lookup fails even though AWS CLI identity and HTTPS probes work.                                                                 | Re-run from a resolver-stable shell/network, verify `gtcx-terraform-locks-staging`, then run remote-state `terraform plan` before any apply.                                 |
| F-INFRA-003 | P2       | local operator                 | watch                     | Local disk is no longer effectively full, but remains high utilization.                                                                                                                         | Earlier `df -h` showed `/System/Volumes/Data` at 100% with ~426 MiB free and transcript had `OSError: [Errno 28]`; current `df -h` shows 24 GiB free at 98% capacity.                                                                                               | Keep at least 20 GiB free before long Terraform/apply/test sessions; avoid large provider/build downloads until free space is stable.                                        |
| F-INFRA-004 | P1       | fabric-os                      | wired / live-empty        | Argo CD is declared in Terraform but not running in staging.                                                                                                                                    | `kubectl get namespace argocd` returns Active; `kubectl get pods -n argocd` returns no resources; staging wires `module "argocd"` and `fabric-staging-root`; `helm list -n argocd` hit EKS endpoint DNS failure.                                                    | After F-INFRA-002 is cleared, install/reconcile Argo CD through Terraform/CodeBuild path only; do not bypass state with direct Helm install.                                 |
| F-INFRA-005 | P1       | fabric-os / baseline-os FinOps | live-warm / drift-check   | Staging EKS is warm at one `t3.medium` while Terraform and FinOps policy declare scheduled staging as cold default.                                                                             | Live nodegroup `gtcx-staging-nodes` is `minSize=1`, `desiredSize=1`, `maxSize=4`; `terraform.tfvars` has `cost_profile = "scheduled"`; cost-profile policy/witness expects staging `0/0/4` with Bridge `env:warm` / `env:cold` runtime controls.                    | After F-INFRA-002 is cleared, remote-state plan should show whether this is state drift or an approved warm override; then run Bridge cold/warm path or Terraform reconcile. |
| F-INFRA-006 | P1       | fabric-os                      | blocked-endpoint          | CodeBuild deploy executor live existence could not be verified from AWS CLI.                                                                                                                    | Expected project is `gtcx-staging-deploy-executor`; Terraform module and staging wiring exist; `aws codebuild batch-get-projects` cannot connect to `https://codebuild.af-south-1.amazonaws.com/` while `curl -I` reaches the endpoint.                             | Retry AWS CLI CodeBuild read from a resolver-stable shell/network; if missing after a successful read, bootstrap only the executor path.                                     |
| F-INFRA-007 | P1       | fabric-os                      | blocked-dynamodb          | Terraform lock state could not be verified because DynamoDB endpoint access fails, though S3 state storage is reachable.                                                                        | `aws dynamodb scan` and `describe-table` cannot connect to `dynamodb.us-east-1.amazonaws.com`; `aws s3api` confirms `gtcx-terraform-state-staging/environments/staging/terraform.tfstate` exists.                                                                   | Verify `gtcx-terraform-locks-staging` from a resolver-stable shell/network before any plan/apply.                                                                            |
| F-INFRA-008 | P1       | nyota-ai / fabric-os           | resolved-pushed           | Nyota Fabric ops contract fix is no longer local-only.                                                                                                                                          | `nyota-ai` `main` is even with `origin/main`; pushed history includes `205a1b6 chore(ops): add fabric contract`; current dirty state is untracked audit artifacts, not the Fabric contract commit.                                                                  | No Fabric action for the contract commit; handle remaining Nyota untracked audit artifacts under F-INFRA-009 / Nyota owner workflow.                                         |
| F-INFRA-009 | P1       | nyota-ai / bridge-os           | confirmed-open            | Nyota compliance/MPR remains below target.                                                                                                                                                      | Fresh Bridge check: compliance `69/100`, 8 violations; MPR exits 2 with foundation `47/100`, 11PR `53/100`; Nyota has untracked audit artifacts that contribute to dated-sibling hygiene findings.                                                                  | In Nyota owner workflow, fix ops-check wiring, ADR version fork, dated witness siblings, shipping-status witness, readiness snapshot, and `pm/ci` gates mapping.             |
| F-INFRA-010 | P1       | bridge-os                      | confirmed-open            | Bridge OS has extensive dirty state and is ahead of origin.                                                                                                                                     | `bridge-os` is ahead 3 (`6919bba2`, `870b6f59`, `f23a81eb`) and has 72 modified tracked files plus untracked audit/script/session artifacts across evidence, specs, and ecosystem scripts.                                                                          | Split/commit/push or isolate Bridge path-migration changes before treating fleet scores as stable.                                                                           |
| F-INFRA-011 | P2       | canon-os / baseline-os         | confirmed-open            | Canon/baseline worktrees are dirty during machine/spec and layout migration work.                                                                                                               | `canon-os` is on `protocol/62-agent-worktree-isolation`, ahead 83/behind 1 with 6 tracked files changed plus untracked `.worktrees`/document outputs; `baseline-os` is on `docs/shell-loop-architecture-spec`, ahead 50/behind 18 with 46 tracked files changed.    | Stabilize, push, or isolate those branches before using Canon/Baseline cross-repo gate claims as stable.                                                                     |
| F-INFRA-012 | P2       | fabric-os                      | confirmed-mixed           | Live AWS endpoint access remains inconsistent from this session.                                                                                                                                | AWS CLI reads work for STS, ACM, EKS, and S3; AWS CLI reads fail for DynamoDB, CodeBuild, Route53, and Terraform backend STS/DynamoDB paths; HTTPS probes resolve the failed service endpoints.                                                                     | Treat live Terraform/apply/deploy as blocked until AWS CLI service reads are consistent from the execution shell or moved into CodeBuild/VPC executor.                       |
| F-INFRA-013 | P2       | ledger-ui / fabric-os          | resolved                  | Fleet aggregate Fabric ops gate failure on `ledger-ui` contract-path drift is resolved.                                                                                                         | `ledger-ui/operations/fabric-contract.json` now matches Fabric central policy contract path `fabric-os/pm/spec/fabric-ops-policy-contract.json`; `pnpm fabric:ops-contracts:check` passes 19/19; `pnpm fabric:operations:check` passes.                             | No further action for this item.                                                                                                                                             |

## Resolved / Not Open

| Item                                                                                      | Evidence                                                                                                                      |
| ----------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| GitHub-independent CI/CD strategy is implemented and pushed.                              | `fabric-os` branch `feat/ai-cost-check` pushed through `6727f21d`.                                                            |
| Fabric deployment ops contract passes locally.                                            | `pnpm deployment:ops:contract:check` exit 0.                                                                                  |
| Workspace/root gate passes locally.                                                       | `pnpm operations:check` exit 0.                                                                                               |
| Product roadmap lane isolation for `fabric-os` passes.                                    | `node platform/scripts/ecosystem/ecosystem-product-roadmap-lane-check.mjs --repo fabric-os` exit 0.                           |
| Class A approval artifact exists for staging deployment ops.                              | `audit/evidence/deployment-ops-class-a-approval-2026-06-30.json`; commit `cceaf508`.                                          |
| Griot ACM validation wait is disabled in staging code to avoid indefinite Terraform wait. | `fabric-os` commit `c310d47d`.                                                                                                |
| Griot DNS authority mismatch has a Terraform remediation path.                            | `deploy/terraform/modules/griot-ai-ingress` supports `dns_provider = "cloudflare"`; staging defaults Griot DNS to Cloudflare. |

## Commands Run

| Command                                                                                                                           | Exit | Result                                                                       |
| --------------------------------------------------------------------------------------------------------------------------------- | ---: | ---------------------------------------------------------------------------- |
| `pnpm fabric:operations:check`                                                                                                    |    0 | Fabric aggregate gate passes.                                                |
| `pnpm operations:check`                                                                                                           |    0 | Workspace/root gate passes.                                                  |
| `terraform fmt -check deploy/terraform/environments/staging deploy/terraform/modules/griot-ai-ingress`                            |    0 | Terraform formatting passes.                                                 |
| `terraform init -backend=false -input=false -no-color`                                                                            |    0 | Staging providers initialized locally; Cloudflare provider installed.        |
| `terraform validate -no-color`                                                                                                    |    0 | Staging Terraform configuration is valid.                                    |
| `pnpm deployment:ops:contract:check`                                                                                              |    0 | Deployment ops contract passes.                                              |
| `terraform plan -input=false -no-color -lock=false`                                                                               |    1 | Failed before plan: STS DNS lookup failed.                                   |
| `AWS_STS_REGIONAL_ENDPOINTS=regional AWS_REGION=af-south-1 terraform plan -input=false -no-color -lock=false`                     |    1 | Failed before plan: regional STS DNS lookup failed.                          |
| `GODEBUG=netdns=cgo AWS_STS_REGIONAL_ENDPOINTS=regional AWS_REGION=af-south-1 terraform plan -input=false -no-color -lock=false`  |    1 | Failed before plan: Terraform still cannot resolve STS.                      |
| `aws sts get-caller-identity`                                                                                                     |    0 | AWS CLI identity resolves and authenticates.                                 |
| `aws dynamodb scan --table-name gtcx-terraform-locks-staging --region us-east-1 --max-items 20`                                   |  255 | Could not connect to DynamoDB endpoint.                                      |
| `curl -I https://sts.amazonaws.com`                                                                                               |    0 | HTTPS probe resolves STS and returns HTTP 302.                               |
| `curl -I https://dynamodb.us-east-1.amazonaws.com`                                                                                |    0 | HTTPS probe resolves DynamoDB and returns HTTP 404.                          |
| `df -h /System/Volumes/Data`                                                                                                      |    0 | Local disk has 24 GiB free; no longer full, still 98% used.                  |
| `aws eks describe-nodegroup ...`                                                                                                  |    0 | Staging node group active, desired/min 1.                                    |
| `kubectl get namespace argocd`                                                                                                    |    0 | Namespace exists and is Active.                                              |
| `kubectl get nodes -o wide`                                                                                                       |    0 | One Ready node.                                                              |
| `kubectl get pods -n argocd`                                                                                                      |    0 | No Argo CD pods.                                                             |
| `helm list -n argocd`                                                                                                             |    1 | EKS endpoint DNS lookup failed before listing releases.                      |
| `aws eks list-nodegroups --cluster-name gtcx-staging --region af-south-1`                                                         |    0 | Live nodegroup is `gtcx-staging-nodes`.                                      |
| `aws eks describe-nodegroup --cluster-name gtcx-staging --nodegroup-name gtcx-staging-nodes --region af-south-1 ...`              |    0 | Live scaling is min 1, desired 1, max 4 on `t3.medium`.                      |
| `terraform test -no-color` in `deploy/terraform/modules/cost-profile`                                                             |    0 | Terraform 1.5.7 reported no test suites; existing witness records 4/4 pass.  |
| `aws codebuild batch-get-projects --names gtcx-staging-deploy-executor --region af-south-1`                                       |  255 | Could not connect to CodeBuild endpoint.                                     |
| `aws codebuild batch-get-projects --names gtcx-production-deploy-executor --region af-south-1`                                    |  255 | Could not connect to CodeBuild endpoint.                                     |
| `curl -I https://codebuild.af-south-1.amazonaws.com`                                                                              |    0 | HTTPS probe reaches CodeBuild endpoint and returns HTTP 404.                 |
| `aws dynamodb describe-table --table-name gtcx-terraform-locks-staging --region us-east-1`                                        |  255 | Could not connect to DynamoDB endpoint.                                      |
| `aws s3api head-bucket --bucket gtcx-terraform-state-staging --region us-east-1`                                                  |    0 | Terraform state bucket exists and is reachable.                              |
| `aws s3api list-objects-v2 --bucket gtcx-terraform-state-staging --prefix environments/staging/ --max-items 5 --region us-east-1` |    0 | Staging state object exists; last modified 2026-06-30T06:28:16Z.             |
| `git -C ../nyota-ai status --short --branch`                                                                                      |    0 | `main...origin/main`; untracked audit artifacts only.                        |
| `git -C ../nyota-ai rev-list --left-right --count origin/main...HEAD`                                                             |    0 | `0 0`; Nyota branch is no longer ahead.                                      |
| `git -C ../nyota-ai log --oneline --decorate -8`                                                                                  |    0 | `205a1b6 chore(ops): add fabric contract` is in pushed history.              |
| `git -C ../bridge-os status --short --branch`                                                                                     |    0 | Bridge is ahead 3 with broad dirty state.                                    |
| `git -C ../bridge-os rev-list --left-right --count origin/main...HEAD`                                                            |    0 | `0 3`; Bridge has three local commits ahead of origin.                       |
| `git -C ../bridge-os diff --stat`                                                                                                 |    0 | 72 tracked files changed, 2469 insertions, 1174 deletions.                   |
| `git -C ../canon-os status --short --branch`                                                                                      |    0 | Canon branch `protocol/62-agent-worktree-isolation` is dirty.                |
| `git -C ../canon-os rev-list --left-right --count origin/main...HEAD`                                                             |    0 | `1 83`; Canon is behind 1 and ahead 83 versus `origin/main`.                 |
| `git -C ../canon-os diff --stat`                                                                                                  |    0 | Canon has 6 tracked files changed, plus untracked worktree/document outputs. |
| `git -C ../baseline-os status --short --branch`                                                                                   |    0 | Baseline branch `docs/shell-loop-architecture-spec` is dirty.                |
| `git -C ../baseline-os rev-list --left-right --count origin/main...HEAD`                                                          |    0 | `18 50`; Baseline is behind 18 and ahead 50 versus `origin/main`.            |
| `git -C ../baseline-os diff --stat`                                                                                               |    0 | Baseline has 46 tracked files changed, 1118 insertions, 481 deletions.       |
| `aws acm list-certificates --region af-south-1 --max-items 1`                                                                     |    0 | ACM CLI read succeeds.                                                       |
| `aws eks list-clusters --region af-south-1`                                                                                       |    0 | EKS CLI read succeeds.                                                       |
| `aws route53 list-hosted-zones-by-name --dns-name gtcx.trade --max-items 2`                                                       |  255 | Route53 CLI read cannot connect to endpoint.                                 |
| `curl -I https://route53.amazonaws.com`                                                                                           |    0 | HTTPS probe reaches Route53 endpoint and returns HTTP 404.                   |
| `pnpm fabric:operations:check`                                                                                                    |    1 | Initial final check failed on `ledger-ui` Fabric ops contract path drift.    |
| `pnpm fabric:ops-contracts:check`                                                                                                 |    0 | After approval, fleet Fabric ops contracts pass 19/19.                       |
| `pnpm fabric:operations:check`                                                                                                    |    0 | After approval, aggregate Fabric operations gate passes.                     |
| `aws acm describe-certificate ...griot...`                                                                                        |    0 | ACM validation pending.                                                      |
| `curl Cloudflare DoH NS gtcx.trade`                                                                                               |    0 | Cloudflare nameservers.                                                      |
| `curl Cloudflare DoH validation CNAME`                                                                                            |    0 | CNAME absent publicly.                                                       |
| `node platform/scripts/ecosystem/check-compliance-rubric.mjs --repo nyota-ai`                                                     |    0 | Compliance 69/100, 8 violations.                                             |
| `node platform/scripts/ecosystem/run-mpr-repo-audit.mjs --repo nyota-ai`                                                          |    2 | Foundation 47/100, 11PR 53/100.                                              |
