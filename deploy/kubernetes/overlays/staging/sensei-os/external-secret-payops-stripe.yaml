# =============================================================================
# sensei-os-staging — PayOps shared Stripe ExternalSecret
# =============================================================================
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: sensei-os-payops-stripe
  namespace: sensei-os-staging
  labels:
    app.kubernetes.io/part-of: gtcx
    app.kubernetes.io/component: sensei-os
    gtcx.io/payops-provider: stripe
    environment: staging
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: SecretStore
    name: sensei-os-aws-secrets
  target:
    name: sensei-os-payops-stripe
    creationPolicy: Owner
    deletionPolicy: Retain
  dataFrom:
    - extract:
        key: gtcx/shared/staging/payops/stripe
