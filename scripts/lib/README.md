# `scripts/lib/` — Shared script helpers

Small modules imported by top-level `scripts/` utilities.

| Module                    | Purpose                                               |
| ------------------------- | ----------------------------------------------------- |
| `attach-launch-focus.mjs` | Attach launch-focus metadata to agent session outputs |

**Parent:** [`scripts/README.md`](../README.md)
