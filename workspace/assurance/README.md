# Assurance workspace

PM v3 local assurance artifacts (`gates.local.json`, `evidence-index.json`) for engineering lane signoff.

Canonical evidence SoR: [`docs/audit/`](../docs/audit/). Sync via `pnpm workspace:check`.
